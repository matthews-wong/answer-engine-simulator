"""answer-engine-simulator: preview how an AI answer engine would respond to a
query using your own site content, and whether it would cite your pages.

This is a portfolio/demo tool. It is a *simulation* — an approximation of
answer-engine behavior built from TF-IDF retrieval plus (optional) Claude
synthesis — not a real search or answer engine.
"""

__version__ = "0.1.0"

__all__ = ["__version__"]
